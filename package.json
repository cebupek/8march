{
  "name": "ping-server",
  "version": "1.0.0",
  "description": "Simple Node.js server with self-ping every second",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
